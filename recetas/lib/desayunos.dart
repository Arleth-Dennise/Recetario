import 'package:flutter/material.dart';

class DesayunosPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Desayunos'),
      ),
      body: Center(
        child: Text('Página de desayunos'),
      ),
    );
  }
}